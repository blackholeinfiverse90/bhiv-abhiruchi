{
  "name": "gurukul-assesment-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "start:server": "npm run start --prefix server",
    "build": "npm run build --prefix client"
  }
}
